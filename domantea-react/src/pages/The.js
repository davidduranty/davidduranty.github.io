import React from 'react';
import Logo from '../components/Logo';
import Navigation from '../components/Navigation';

const The = () => {
    return (
        <div>
            <Logo />
            <Navigation/>
            <h1>THES</h1>
        </div>
    );
};

export default The;